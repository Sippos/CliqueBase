import { useEffect, useMemo, useRef, useState } from 'react'
import { useParams } from 'react-router-dom'
import AppIcon from '../components/AppIcon.jsx'
import MemberShareModal from '../components/MemberShareModal.jsx'
import PageShell from '../components/PageShell.jsx'
import { GROUPS_CHANGED_EVENT, getActiveGroup, getActiveGroupId, setActiveGroup } from '../lib/groups.js'
import { getSavedHandle } from '../lib/handle.js'
import { getCurrentSession, getGames, getMovies, getRemoteGroups, getSeries, hasSupabase, saveGame, saveMovie, saveSeries, voteGame, voteMovie, voteSeries } from '../lib/supabaseClient.js'

const TYPE_ICONS = { Movie: 'movies', Series: 'series', Game: 'games' }

function normalizeItems(rows, type, code) {
  return rows.map((item) => ({ ...item, type, code, rating: item.rating || null, sortValue: Number(item.score || 0) * 10 + Number(item.picks || 0) + Number(item.rating || 0) })).sort((a, b) => b.sortValue - a.sortValue)
}
function itemActionKey(item, prefix = '') { return item ? `${prefix}${item.type}-${item.id}` : '' }
function itemText(item) { return item?.overview || item?.description || 'No description yet.' }
function plural(value, singular, pluralLabel = `${singular}s`) { return `${value} ${Number(value) === 1 ? singular : pluralLabel}` }

function SmallIconButton({ icon, label, onClick, disabled = false, className = '', strong = false }) {
  return (
    <button type="button" aria-label={label} title={label} onClick={onClick} disabled={disabled} className={`inline-flex h-10 w-10 items-center justify-center rounded-full border text-white backdrop-blur transition disabled:pointer-events-none disabled:opacity-50 ${strong ? 'border-white bg-white text-neutral-950 shadow-lg shadow-black/25 hover:bg-neutral-200' : 'border-white/15 bg-black/55 hover:bg-white hover:text-neutral-950'} ${className}`}>
      <AppIcon name={icon} size={16} strokeWidth={2.4} />
    </button>
  )
}

function OverviewMetric({ icon, label, value, detail, onClick }) {
  const body = <><span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-2xl border border-white/10 bg-white/[0.05] text-neutral-200"><AppIcon name={icon} size={17} strokeWidth={2.3} /></span><span className="min-w-0 flex-1"><span className="block text-[10px] font-black uppercase tracking-[0.22em] text-neutral-500">{label}</span><span className="mt-1 block text-xl font-black leading-none text-white">{value}</span>{detail ? <span className="mt-1 block truncate text-xs text-neutral-500">{detail}</span> : null}</span></>
  if (onClick) return <button type="button" onClick={onClick} className="flex min-w-0 items-center gap-3 rounded-2xl border border-white/10 bg-white/[0.03] p-3 text-left transition hover:-translate-y-0.5 hover:border-white/25 hover:bg-white/[0.06]">{body}</button>
  return <div className="flex min-w-0 items-center gap-3 rounded-2xl border border-white/10 bg-white/[0.03] p-3">{body}</div>
}

function CategoryOverviewRow({ category, loading, onOpen }) {
  const topTitle = category.top?.title || `No ${category.title.toLowerCase()} yet`
  const picks = category.items.reduce((sum, item) => sum + Number(item.picks || 0), 0)
  return <button type="button" onClick={() => onOpen?.(category)} className="group flex min-w-0 items-center gap-3 rounded-2xl border border-white/10 bg-neutral-950/70 p-3 text-left transition hover:-translate-y-0.5 hover:border-white/25 hover:bg-white/[0.04]"><span className="flex h-11 w-11 shrink-0 items-center justify-center rounded-2xl bg-white text-neutral-950 shadow-lg shadow-black/20"><AppIcon name={category.icon} size={19} strokeWidth={2.5} /></span><span className="min-w-0 flex-1"><span className="flex items-center gap-2 text-sm font-black text-white">{category.title}<span className="rounded-full border border-white/10 px-2 py-0.5 text-[10px] uppercase tracking-[0.16em] text-neutral-400">{loading ? '…' : plural(category.count, 'item')}</span></span><span className="mt-1 block truncate text-sm text-neutral-400">{loading ? 'Loading section…' : topTitle}</span></span><span className="hidden shrink-0 text-right sm:block"><span className="block text-sm font-black text-white">{loading ? '…' : picks}</span><span className="text-[10px] uppercase tracking-[0.18em] text-neutral-500">picks</span></span><AppIcon name="chevronRight" size={16} className="shrink-0 text-neutral-500 transition group-hover:text-white" /></button>
}

function LibraryOverviewPanel({ items, categories, loading, ratedCount, totalPicks, onOpen }) {
  return <section className="mt-5 rounded-[1.6rem] border border-white/10 bg-neutral-950/70 p-3 shadow-2xl shadow-black/20"><div className="flex flex-wrap items-end justify-between gap-3 px-1"><div><p className="text-[10px] font-black uppercase tracking-[0.3em] text-neutral-500">Overview</p><h2 className="mt-1 text-xl font-black text-white">Your library at a glance</h2></div><p className="max-w-xs text-sm leading-6 text-neutral-500">Small counters first, section details underneath.</p></div><div className="mt-3 grid gap-2 sm:grid-cols-2 xl:grid-cols-3"><OverviewMetric icon="dashboard" label="Total" value={loading ? '…' : items.length} detail="all saved media" />{categories.map((category) => <OverviewMetric key={category.title} icon={category.icon} label={category.title} value={loading ? '…' : category.count} detail={category.top?.title || 'empty section'} onClick={() => onOpen?.(category)} />)}<OverviewMetric icon="info" label="Rated" value={loading ? '…' : ratedCount} detail="with rating data" /><OverviewMetric icon="users" label="Picks" value={loading ? '…' : totalPicks} detail="saved votes" /></div><div className="mt-3 grid gap-2 lg:grid-cols-3">{categories.map((category) => <CategoryOverviewRow key={category.title} category={category} loading={loading} onOpen={onOpen} />)}</div></section>
}

function LibraryShowcase({ items, loading, onShare, onInfo }) {
  const [index, setIndex] = useState(0)
  useEffect(() => { setIndex((current) => items.length ? Math.min(current, items.length - 1) : 0) }, [items.length])
  useEffect(() => { if (items.length < 2) return undefined; const timer = window.setInterval(() => setIndex((current) => (current + 1) % items.length), 4200); return () => window.clearInterval(timer) }, [items.length])
  if (loading) return <div className="relative flex min-h-[260px] items-end overflow-hidden bg-neutral-950 p-5 xl:min-h-full"><div className="absolute inset-0 animate-pulse bg-[radial-gradient(circle_at_30%_20%,rgba(255,255,255,0.12),transparent_32%),linear-gradient(135deg,rgba(255,255,255,0.06),rgba(0,0,0,0.45))]" /><div className="relative"><p className="text-xs uppercase tracking-[0.3em] text-neutral-500">Library feel</p><h2 className="mt-2 text-2xl font-black text-white">Loading your library…</h2></div></div>
  if (!items.length) return <div className="relative flex min-h-[260px] items-end overflow-hidden bg-[radial-gradient(circle_at_30%_20%,rgba(255,255,255,0.16),transparent_32%),linear-gradient(135deg,rgba(255,255,255,0.08),rgba(0,0,0,0.4))] p-5 xl:min-h-full"><div><p className="text-xs uppercase tracking-[0.3em] text-neutral-400">Empty library</p><h2 className="mt-2 text-2xl font-black text-white">Add the first item</h2><p className="mt-2 max-w-sm text-sm leading-6 text-neutral-300">Watched movies, finished series, and played games will appear here once you save them.</p></div></div>
  const item = items[index] || items[0]
  const image = item.backdrop || item.poster
  return <div className="relative min-h-[260px] overflow-hidden bg-neutral-950 xl:min-h-full"><button type="button" onClick={() => onInfo?.(item)} className="group absolute inset-0 flex items-end p-5 text-left">{image ? <img src={image} alt="" className="absolute inset-0 h-full w-full object-cover opacity-65 transition duration-700 group-hover:scale-105" /> : <div className="absolute inset-0 bg-[radial-gradient(circle_at_30%_20%,rgba(255,255,255,0.16),transparent_32%),linear-gradient(135deg,rgba(255,255,255,0.08),rgba(0,0,0,0.4))]" />}<div className="absolute inset-0 bg-gradient-to-t from-black via-black/55 to-black/10" /><div className="absolute left-5 top-5 right-5 flex items-center justify-between gap-3"><span className="rounded-full border border-white/15 bg-black/35 px-3 py-1.5 text-xs font-black uppercase tracking-[0.22em] text-white backdrop-blur">Library reel</span><span className="rounded-full border border-white/15 bg-black/35 px-3 py-1.5 text-xs font-bold text-neutral-200 backdrop-blur">{index + 1}/{items.length}</span></div><div className="relative max-w-md pr-20"><p className="text-xs uppercase tracking-[0.3em] text-neutral-300">{item.type}</p><h2 className="mt-2 text-2xl font-black text-white sm:text-3xl">{item.title}</h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-neutral-300">{itemText(item)}</p></div></button><div className="absolute bottom-5 right-5 z-10 flex gap-2"><SmallIconButton icon="info" label={`Show details for ${item.title}`} onClick={() => onInfo?.(item)} /><SmallIconButton icon="share" label={`Share ${item.title}`} onClick={() => onShare?.(item)} strong /></div></div>
}

function CategorySpotlightCard({ category, loading, isClique, saving, index, onCycle, onOpenPile, onInfo, onShare, onCopy }) {
  const dragStartRef = useRef(null)
  const draggedRef = useRef(false)
  const wheelLockRef = useRef(0)
  const items = category.items || []
  const safeIndex = items.length ? index % items.length : 0
  const item = items[safeIndex]
  const image = item?.backdrop || item?.poster
  const canCycle = items.length > 1
  const title = item?.title || `No ${category.title.toLowerCase()} yet`
  function cycle(direction) { if (canCycle) onCycle?.(category, direction) }
  function handleWheel(event) { if (!canCycle) return; const delta = Math.abs(event.deltaY) >= Math.abs(event.deltaX) ? event.deltaY : event.deltaX; if (Math.abs(delta) < 10) return; const now = Date.now(); if (now - wheelLockRef.current < 180) return; wheelLockRef.current = now; event.preventDefault(); event.stopPropagation(); cycle(delta > 0 ? 1 : -1) }
  function handlePointerDown(event) { dragStartRef.current = { x: event.clientX, y: event.clientY }; draggedRef.current = false }
  function handlePointerMove(event) { const start = dragStartRef.current; if (!start || !canCycle) return; const dx = event.clientX - start.x; const dy = event.clientY - start.y; if (Math.abs(dx) > 18 && Math.abs(dx) > Math.abs(dy) * 1.15) draggedRef.current = true }
  function handlePointerUp(event) { const start = dragStartRef.current; dragStartRef.current = null; if (!start || !canCycle) return; const dx = event.clientX - start.x; const dy = event.clientY - start.y; if (Math.abs(dx) > 48 && Math.abs(dx) > Math.abs(dy) * 1.2) { event.preventDefault(); event.stopPropagation(); draggedRef.current = true; cycle(dx < 0 ? 1 : -1) } }
  function openMain() { if (draggedRef.current) { draggedRef.current = false; return } item ? onInfo?.(item) : onOpenPile?.(category) }
  return <article onWheel={handleWheel} className="group relative min-h-[24rem] overflow-hidden rounded-[1.75rem] border border-white/10 bg-neutral-950 text-white shadow-2xl shadow-black/20 transition hover:-translate-y-0.5 hover:border-white/25">{image ? <img src={image} alt="" draggable="false" className="absolute inset-0 h-full w-full object-cover opacity-82 transition duration-500 group-hover:scale-105" /> : <div className="absolute inset-0 bg-[radial-gradient(circle_at_30%_20%,rgba(255,255,255,0.14),transparent_32%),linear-gradient(135deg,rgba(255,255,255,0.08),rgba(0,0,0,0.45))]" />}<div className="absolute inset-0 bg-gradient-to-t from-black via-black/45 to-black/5" /><button type="button" onClick={openMain} onPointerDown={handlePointerDown} onPointerMove={handlePointerMove} onPointerUp={handlePointerUp} onPointerCancel={() => { dragStartRef.current = null }} className="absolute inset-0 z-10 touch-pan-y text-left" aria-label={`Open ${title}`} /><div className="pointer-events-none relative z-20 flex min-h-[24rem] flex-col justify-between p-5"><div className="flex items-start justify-between gap-3"><span className="inline-flex items-center gap-2 rounded-full bg-white px-3 py-1.5 text-[10px] font-black uppercase tracking-[0.14em] text-neutral-950"><AppIcon name={category.icon} size={12} />{category.title}</span><div className="pointer-events-auto flex flex-wrap justify-end gap-2">{canCycle ? <SmallIconButton icon="chevronLeft" label={`Previous ${category.singular}`} onClick={(event) => { event.stopPropagation(); cycle(-1) }} /> : null}{canCycle ? <SmallIconButton icon="chevronRight" label={`Next ${category.singular}`} onClick={(event) => { event.stopPropagation(); cycle(1) }} /> : null}<button type="button" onClick={(event) => { event.stopPropagation(); onOpenPile?.(category) }} className="inline-flex h-10 items-center gap-1.5 rounded-full border border-white/15 bg-black/55 px-3 text-xs font-black text-white backdrop-blur hover:bg-white hover:text-neutral-950"><AppIcon name="list" size={14} />{category.count}</button>{item ? <SmallIconButton icon="info" label={`Show details for ${item.title}`} onClick={(event) => { event.stopPropagation(); onInfo?.(item) }} /> : null}</div></div><div className="pointer-events-auto rounded-[1.5rem] border border-white/10 bg-black/65 p-4 shadow-2xl shadow-black/30 backdrop-blur-md"><p className="text-[10px] font-black uppercase tracking-[0.24em] text-neutral-300">#{safeIndex + 1} in {category.title}</p><h3 className="mt-2 line-clamp-2 text-3xl font-black leading-tight text-white">{loading ? 'Loading section…' : title}</h3><div className="mt-3 flex flex-wrap gap-2 text-xs font-semibold text-neutral-200"><span className="rounded-full border border-white/15 bg-black/35 px-3 py-1.5">Score {item?.score || 0}</span><span className="rounded-full border border-white/15 bg-black/35 px-3 py-1.5">{item?.picks || 0} picks</span>{item?.rating ? <span className="rounded-full border border-white/15 bg-black/35 px-3 py-1.5">Rating {Number(item.rating).toFixed(1)}</span> : null}</div><p className="mt-3 line-clamp-3 text-sm leading-6 text-neutral-300">{item ? itemText(item) : `Add ${category.title.toLowerCase()} to make this section feel alive.`}</p><div className="mt-4 flex flex-wrap items-center justify-between gap-2"><button type="button" onClick={(event) => { event.stopPropagation(); onOpenPile?.(category) }} className="inline-flex items-center gap-2 rounded-2xl border border-white/10 px-3 py-2 text-xs font-black uppercase tracking-[0.14em] text-white transition hover:bg-white hover:text-neutral-950"><AppIcon name="list" size={14} />Open list</button><div className="flex gap-2">{item ? <SmallIconButton icon="share" label={`Share ${item.title}`} onClick={(event) => { event.stopPropagation(); onShare?.(item) }} /> : null}{isClique && item ? <SmallIconButton icon="copy" label={`Copy ${item.title} to My Library`} onClick={(event) => { event.stopPropagation(); onCopy?.(item) }} disabled={saving} strong /> : null}</div></div>{canCycle ? <p className="mt-3 text-[10px] font-black uppercase tracking-[0.18em] text-neutral-600">Swipe/drag sideways on mobile · mouse wheel or arrows on desktop</p> : null}</div></div></article>
}

function LibraryListPanel({ category, loading, isClique, votingKey, copyingKey, onClose, onVote, onInfo, onShare, onCopy }) {
  if (!category) return null
  const items = category.items || []
  return <section className="mt-5 rounded-[2rem] border border-white/10 bg-white/[0.03] p-5" id="library-inline-list"><div className="flex flex-wrap items-start justify-between gap-3 border-b border-white/10 pb-5"><div><p className="inline-flex items-center gap-2 text-xs font-black uppercase tracking-[0.24em] text-neutral-500"><AppIcon name={category.icon} size={14} />{isClique ? 'Clique list' : 'Library list'}</p><h2 className="mt-1 text-3xl font-black text-white">{category.title}</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-neutral-400">{loading ? 'Loading…' : plural(items.length, 'item')} in this section.</p></div><button type="button" onClick={onClose} className="rounded-2xl border border-white/10 px-4 py-2 text-sm font-black text-neutral-300 transition hover:bg-white hover:text-neutral-950">Close</button></div>{loading ? <p className="mt-5 rounded-3xl border border-white/10 p-5 text-sm text-neutral-400">Loading list…</p> : items.length ? <div className="mt-5 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{items.map((item, index) => { const image = item.backdrop || item.poster; const voteBusy = votingKey === itemActionKey(item, 'vote-'); const copyBusy = copyingKey === itemActionKey(item, 'copy-'); return <article key={`${item.type}-${item.id}`} className="overflow-hidden rounded-[1.5rem] border border-white/10 bg-neutral-950/80"><button type="button" onClick={() => onInfo?.(item)} className="group relative block h-44 w-full overflow-hidden text-left">{image ? <img src={image} alt="" className="absolute inset-0 h-full w-full object-cover opacity-75 transition duration-500 group-hover:scale-105" /> : <div className="absolute inset-0 bg-[radial-gradient(circle_at_30%_20%,rgba(255,255,255,0.14),transparent_32%),linear-gradient(135deg,rgba(255,255,255,0.08),rgba(0,0,0,0.45))]" />}<div className="absolute inset-0 bg-gradient-to-t from-black via-black/45 to-transparent" /><span className="absolute left-3 top-3 rounded-full border border-white/15 bg-black/55 px-3 py-1 text-xs font-black uppercase tracking-[0.18em] text-white backdrop-blur">#{index + 1}</span><div className="absolute inset-x-0 bottom-0 p-4"><p className="text-xs uppercase tracking-[0.22em] text-neutral-300">{item.type}</p><h3 className="mt-1 line-clamp-2 text-2xl font-black leading-tight text-white">{item.title}</h3></div></button><div className="p-4"><p className="line-clamp-2 text-sm leading-6 text-neutral-400">{itemText(item)}</p><div className="mt-3 flex flex-wrap gap-2 text-xs font-semibold text-neutral-300"><span className="rounded-full border border-white/10 px-3 py-1.5">Score {item.score || 0}</span><span className="rounded-full border border-white/10 px-3 py-1.5">{item.picks || 0} picks</span>{item.rating ? <span className="rounded-full border border-white/10 px-3 py-1.5">★ {Number(item.rating).toFixed(1)}</span> : null}</div><div className="mt-4 flex flex-wrap gap-2"><button type="button" onClick={() => onInfo?.(item)} className="rounded-2xl border border-white/10 px-3 py-2 text-sm font-black text-white transition hover:bg-white hover:text-neutral-950">Info</button><button type="button" onClick={() => onShare?.(item)} className="rounded-2xl border border-white/10 px-3 py-2 text-sm font-black text-white transition hover:bg-white hover:text-neutral-950">Share</button>{isClique ? <button type="button" disabled={copyBusy} onClick={() => onCopy?.(item)} className="rounded-2xl border border-white/10 px-3 py-2 text-sm font-black text-white transition hover:bg-white hover:text-neutral-950 disabled:opacity-60">{copyBusy ? 'Copying…' : 'Copy'}</button> : null}{isClique ? <button type="button" disabled={voteBusy} onClick={() => onVote?.(item, 'like')} className="rounded-2xl bg-white px-3 py-2 text-sm font-black text-neutral-950 transition hover:bg-neutral-200 disabled:opacity-60">{voteBusy ? 'Saving…' : 'Watch'}</button> : null}{isClique ? <button type="button" disabled={voteBusy} onClick={() => onVote?.(item, 'dislike')} className="rounded-2xl border border-white/10 px-3 py-2 text-sm font-black text-white transition hover:bg-white hover:text-neutral-950 disabled:opacity-60">Pass</button> : null}</div></div></article> })}</div> : <p className="mt-5 rounded-3xl border border-dashed border-white/10 p-5 text-sm leading-6 text-neutral-400">No {category.title.toLowerCase()} saved here yet.</p>}</section>
}

function ItemInfoModal({ item, onClose }) {
  if (!item) return null
  const image = item.backdrop || item.poster
  const icon = TYPE_ICONS[item.type] || 'explore'
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/70 p-4 backdrop-blur-sm"><div className="max-h-[88vh] w-full max-w-2xl overflow-y-auto rounded-[2rem] border border-white/10 bg-neutral-950 p-5 text-white shadow-2xl shadow-black/40"><div className="flex items-start justify-between gap-4"><div className="min-w-0"><p className="inline-flex items-center gap-2 text-xs font-black uppercase tracking-[0.24em] text-neutral-500"><AppIcon name={icon} size={14} />{item.type}</p><h2 className="mt-2 text-2xl font-black leading-tight">{item.title}</h2></div><button type="button" onClick={onClose} className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full border border-white/10 text-2xl text-neutral-400 transition hover:bg-white hover:text-neutral-950">×</button></div>{image ? <img src={image} alt="" className="mt-5 h-56 w-full rounded-3xl object-cover" /> : null}<div className="mt-4 flex flex-wrap gap-2 text-xs font-semibold text-neutral-300"><span className="rounded-full border border-white/10 px-3 py-1.5">Score {item.score || 0}</span><span className="rounded-full border border-white/10 px-3 py-1.5">{item.picks || 0} picks</span>{item.rating ? <span className="rounded-full border border-white/10 px-3 py-1.5">★ {Number(item.rating).toFixed(1)}</span> : null}{item.runtime ? <span className="rounded-full border border-white/10 px-3 py-1.5">{item.runtime} min</span> : null}{item.seasons ? <span className="rounded-full border border-white/10 px-3 py-1.5">{item.seasons} seasons</span> : null}</div><p className="mt-5 text-sm leading-7 text-neutral-300">{itemText(item)}</p></div></div>
}

export default function Home() {
  const { groupId: routeGroupId } = useParams()
  const [loading, setLoading] = useState(hasSupabase)
  const [status, setStatus] = useState(hasSupabase ? 'checking' : 'local')
  const [context, setContext] = useState(() => ({ type: getActiveGroup() ? 'group' : 'personal', name: getActiveGroup()?.name || 'My Library', groupId: getActiveGroup()?.id || null }))
  const [media, setMedia] = useState({ movies: [], series: [], games: [] })
  const [message, setMessage] = useState('')
  const [shareNotice, setShareNotice] = useState('')
  const [sharingItem, setSharingItem] = useState(null)
  const [infoItem, setInfoItem] = useState(null)
  const [activePileTitle, setActivePileTitle] = useState('')
  const [copyingKey, setCopyingKey] = useState('')
  const [votingKey, setVotingKey] = useState('')
  const [spotlightIndexes, setSpotlightIndexes] = useState({})
  useEffect(() => { if (routeGroupId) setActiveGroup(routeGroupId); refreshDashboard(routeGroupId || null); function handleGroupChange() { refreshDashboard(routeGroupId || null) } window.addEventListener(GROUPS_CHANGED_EVENT, handleGroupChange); return () => window.removeEventListener(GROUPS_CHANGED_EVENT, handleGroupChange) }, [routeGroupId])
  const movieItems = useMemo(() => normalizeItems(media.movies, 'Movie', 'MOV'), [media.movies])
  const seriesItems = useMemo(() => normalizeItems(media.series, 'Series', 'SER'), [media.series])
  const gameItems = useMemo(() => normalizeItems(media.games, 'Game', 'GAM'), [media.games])
  const allItems = useMemo(() => [...movieItems, ...seriesItems, ...gameItems].sort((a, b) => b.sortValue - a.sortValue), [movieItems, seriesItems, gameItems])
  const categories = useMemo(() => [{ title: 'Movies', singular: 'Movie', code: 'MOV', icon: TYPE_ICONS.Movie, groupId: context.groupId, items: movieItems, top: movieItems[0], count: movieItems.length, rated: movieItems.filter((item) => item.rating).length }, { title: 'Series', singular: 'Series', code: 'SER', icon: TYPE_ICONS.Series, groupId: context.groupId, items: seriesItems, top: seriesItems[0], count: seriesItems.length, rated: seriesItems.filter((item) => item.rating).length }, { title: 'Games', singular: 'Game', code: 'GAM', icon: TYPE_ICONS.Game, groupId: context.groupId, items: gameItems, top: gameItems[0], count: gameItems.length, rated: gameItems.filter((item) => item.rating).length }], [context.groupId, movieItems, seriesItems, gameItems])
  const activePile = useMemo(() => categories.find((category) => category.title === activePileTitle) || null, [categories, activePileTitle])
  const ratedCount = useMemo(() => allItems.filter((item) => item.rating).length, [allItems])
  const totalPicks = useMemo(() => allItems.reduce((sum, item) => sum + Number(item.picks || 0), 0), [allItems])
  const isClique = Boolean(context.groupId)
  async function refreshDashboard(preferredGroupId = null) { setLoading(true); setMessage(''); if (!hasSupabase) { const group = preferredGroupId ? setActiveGroup(preferredGroupId) : getActiveGroup(); setContext({ type: group ? 'group' : 'personal', name: group?.name || 'My Library', groupId: group?.id || null }); setMedia({ movies: [], series: [], games: [] }); setStatus('local'); setLoading(false); return } try { const session = await getCurrentSession(); if (!session?.user) { setStatus('signed-out'); setContext({ type: 'personal', name: 'My Library', groupId: null }); setMedia({ movies: [], series: [], games: [] }); setLoading(false); return } const remoteGroups = await getRemoteGroups().catch(() => []); const activeId = preferredGroupId || getActiveGroupId(); const group = remoteGroups.find((item) => item.id === activeId) || null; const localGroup = getActiveGroup(); const groupId = activeId || null; if (groupId) setActiveGroup(groupId); setContext({ type: groupId ? 'group' : 'personal', name: group?.name || (groupId && localGroup?.id === groupId ? localGroup.name : null) || (groupId ? 'Clique' : 'My Library'), groupId }); const [movies, seriesRows, games] = await Promise.all([getMovies(groupId), getSeries(groupId), getGames(groupId)]); setMedia({ movies, series: seriesRows, games }); setStatus('ready') } catch (error) { setStatus('error'); setMessage(error.message || 'Could not load this workspace.'); setMedia({ movies: [], series: [], games: [] }) } finally { setLoading(false) } }
  function openShare(item) { setSharingItem(item) }
  function handleShareMessage(text) { setShareNotice(text); setTimeout(() => setShareNotice(''), 2600) }
  function openPile(category) { setActivePileTitle(category.title); window.setTimeout(() => document.getElementById('library-inline-list')?.scrollIntoView({ behavior: 'smooth', block: 'start' }), 50) }
  function cycleSpotlight(category, direction) { if (!category?.items?.length) return; setSpotlightIndexes((current) => { const previous = current[category.title] || 0; const next = (previous + direction + category.items.length) % category.items.length; return { ...current, [category.title]: next } }) }
  async function copyToLibrary(item) { if (!item || !hasSupabase || status !== 'ready') return handleShareMessage('Sign in from Profile before copying to My Library.'); const key = itemActionKey(item, 'copy-'); setCopyingKey(key); try { const nominatedBy = getSavedHandle() || 'anonymous'; if (item.type === 'Movie') await saveMovie(item, nominatedBy, null); else if (item.type === 'Series') await saveSeries(item, nominatedBy, null); else if (item.type === 'Game') await saveGame(item, nominatedBy, null); else throw new Error('Unsupported item type.'); handleShareMessage(`Copied "${item.title}" to My Library.`) } catch (error) { handleShareMessage(error.message || 'Could not copy this item to My Library.') } finally { setCopyingKey('') } }
  async function voteInClique(item, vote) { if (!item || !context.groupId || !hasSupabase || status !== 'ready') return handleShareMessage('Open a clique first to vote.'); const key = itemActionKey(item, 'vote-'); setVotingKey(key); try { if (item.type === 'Movie') await voteMovie(item, vote, context.groupId); else if (item.type === 'Series') await voteSeries(item, vote, context.groupId); else if (item.type === 'Game') await voteGame(item, vote, context.groupId); else throw new Error('Unsupported item type.'); handleShareMessage(vote === 'like' ? `Voted to watch "${item.title}".` : `Passed on "${item.title}".`); await refreshDashboard(context.groupId) } catch (error) { handleShareMessage(error.message || 'Could not save your vote.') } finally { setVotingKey('') } }
  return <PageShell active={isClique ? 'cliques' : 'library'}><section className="overflow-hidden rounded-[2rem] border border-white/10 bg-white/[0.03] shadow-2xl shadow-black/20"><div className="grid gap-0 xl:grid-cols-[1.1fr_0.9fr]"><div className="p-4 sm:p-6"><p className="text-xs font-black uppercase tracking-[0.3em] text-neutral-500">{isClique ? 'Clique' : 'Personal library'}</p><h1 className="mt-2 max-w-3xl text-3xl font-black tracking-tight text-white sm:text-5xl">{context.name}</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-neutral-400 sm:text-base">{isClique ? 'Shared movie, series, and game picks for this clique.' : 'Your watched movies, finished series, and played games in one clean overview.'}</p>{status === 'signed-out' ? <p className="mt-3 inline-flex rounded-full border border-white/10 bg-white/[0.06] px-4 py-2 text-sm text-neutral-300">Sign in from Profile to save picks and sync your library.</p> : null}<LibraryOverviewPanel items={loading ? [] : allItems} categories={categories} loading={loading} ratedCount={ratedCount} totalPicks={totalPicks} onOpen={openPile} /></div><LibraryShowcase items={loading ? [] : allItems} loading={loading} onShare={openShare} onInfo={setInfoItem} /></div></section>{shareNotice ? <div className="mt-5 rounded-2xl border border-emerald-400/30 bg-emerald-950/30 p-4 text-sm text-emerald-100">{shareNotice}</div> : null}{message ? <div className="mt-5 rounded-2xl border border-rose-400/30 bg-rose-950/30 p-4 text-sm text-rose-100">{message}</div> : null}<section className="mt-5 rounded-[2rem] border border-white/10 bg-white/[0.03] p-5"><div><p className="text-xs uppercase tracking-[0.3em] text-neutral-500">Section highlights</p><h2 className="mt-1 text-3xl font-black text-white">Top items by section</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-neutral-400">Cards now match the clique overview style. Use arrows, mouse wheel, or swipe/drag sideways to move through each section.</p></div><div className="mt-5 grid gap-4 lg:grid-cols-3">{categories.map((category) => { const index = spotlightIndexes[category.title] || 0; const item = category.items[index % Math.max(category.items.length, 1)] || category.top; return <CategorySpotlightCard key={category.title} category={category} index={index} loading={loading} isClique={isClique} saving={copyingKey === itemActionKey(item, 'copy-')} onCycle={cycleSpotlight} onOpenPile={openPile} onInfo={setInfoItem} onShare={openShare} onCopy={copyToLibrary} /> })}</div></section><LibraryListPanel category={activePile} loading={loading} isClique={isClique} votingKey={votingKey} copyingKey={copyingKey} onClose={() => setActivePileTitle('')} onVote={voteInClique} onInfo={setInfoItem} onShare={openShare} onCopy={copyToLibrary} /><ItemInfoModal item={infoItem} onClose={() => setInfoItem(null)} /><MemberShareModal item={sharingItem} type={sharingItem?.type?.toLowerCase()} onClose={() => setSharingItem(null)} onMessage={handleShareMessage} /></PageShell>
}
